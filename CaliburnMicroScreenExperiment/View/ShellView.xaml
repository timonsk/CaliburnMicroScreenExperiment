<Window x:Class="CaliburnMicroScreenExperiment.View.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:control="clr-namespace:CaliburnMicroScreenExperiment.View"
        xmlns:cal="http://www.caliburnproject.org"
        Title="Shell"
        Width="525"
        Height="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding DisplayControl}" >
            
            </ContentControl>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button Click="ButtonBase_OnClick" Content="Next step" />

            </StackPanel>
        </Grid>
    </Grid>
</Window>
